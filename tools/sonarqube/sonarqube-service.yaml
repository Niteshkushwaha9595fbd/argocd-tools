apiVersion: v1
kind: Service
metadata:
  name: sonarqube-service
  namespace: sonarqube
  labels:
    app: sonarqube-server
spec:
  type: ClusterIP
  selector:
    app: sonarqube-server
  ports:
    - name: http
      port: 9000
      targetPort: 9000
      protocol: TCP
